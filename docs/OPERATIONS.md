# Operations

Keep the default network-denied policy and run DGM only with the supported sandbox executable. Inspect audit events after recovery, license refusals, egress refusals, and persistence corruption. Capture release metadata and checksums with every deployment. Do not place secrets in environment variables visible to untrusted jobs.

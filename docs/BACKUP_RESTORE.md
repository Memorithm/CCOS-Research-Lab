# Backup and Restore

Back up verified snapshots, oplogs, cold blobs, indexes, and the format metadata together. Restore into a disposable directory, verify hashes, and replay before replacing live state. If encrypted storage is enabled, key loss makes the backup unrecoverable; keep keys in an OS-managed or separately protected store.

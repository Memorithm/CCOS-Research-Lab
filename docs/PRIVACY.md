# Privacy

CCOS does not phone home or emit telemetry by default. Logs and metrics must not contain license tokens, machine fingerprints, or source content. Local state directories are created with restrictive permissions where supported. Filesystem deletion cannot guarantee physical erasure; use encrypted storage and OS disk controls for stronger guarantees.

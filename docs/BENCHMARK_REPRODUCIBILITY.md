# Benchmark Reproducibility

`scripts/benchmark_repro.sh` records commit, compiler, target, feature set, seed, configuration, raw JSON, Markdown, and SHA-256 checksums under a provenance directory. Benchmark numbers are observations, not correctness gates; retain historical artifacts instead of overwriting them.

# ┌──────────────────────────────────────────────────────────────────────┐
# │  EXPERIMENTAL — OUTSIDE CERTIFIABLE PRODUCT BOUNDARY                 │
# │  CCOS Research Lab — sandbox & candidate-execution safety gate.      │
# │  HARD RULE: never run this on a runner containing API keys, GitHub   │
# │  write tokens, SSH keys, Docker socket access, production repos, or  │
# │  Enterprise secrets (docs/EXPERIMENT_SAFETY.md).                     │
# └──────────────────────────────────────────────────────────────────────┘
name: sandbox-safety

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: sandbox-safety-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sandbox:
    name: sandbox + generated-code execution safety
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Install stable Rust
        run: rustup toolchain install stable --profile minimal

      - name: no-secret posture (job env must be empty of tokens)
        run: |
          ! env | grep -iE 'token|secret|api[_-]?key|password'
          echo "no secrets present in job environment"

      # Dependency acquisition is infrastructure bootstrap, not candidate
      # evaluation. Populate Cargo's registry/git caches explicitly while the
      # read-only job still has network, then make every sandbox/evaluator step
      # below prove that it succeeds with Cargo offline.
      - name: bootstrap locked dependencies (network allowed only here)
        run: cargo fetch --locked

      - name: sandbox unit tests (fail-closed + hermetic runner policy)
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test --release -p ccos-sandbox --locked -- --nocapture

      - name: candidate protocol + sealed evaluator (std-only tier)
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test --release -p rsi --locked candidate_ -- --nocapture

      - name: canonical SciRust execution attestation gate
        env:
          CARGO_NET_OFFLINE: "true"
        run: >-
          cargo test --release -p rsi
          --features execution-attestation --locked
          execution_attestation -- --nocapture

      - name: repeated-seed disjoint holdout promotion gate
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test --release -p rsi --locked repeated_seed -- --nocapture

      - name: CCOS primary-log candidate + evidence + adoption chain
        env:
          CARGO_NET_OFFLINE: "true"
        run: >-
          cargo test --release -p ccos-research-lab
          --features rsi --locked candidate_bridge -- --nocapture

      - name: guarded DGM tier compiles locked
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo check --release -p ccos-research-lab --features rsi-dgm --locked

      - name: Forge hermetic isolation helpers
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test --release -p forge-core --locked hermetic -- --nocapture

      - name: sandbox-elite injection tests (forge)
        env:
          CARGO_NET_OFFLINE: "true"
        run: cargo test --release -p forge-core sandbox --locked -- --nocapture

      - name: no-network during evaluation
        env:
          CARGO_NET_OFFLINE: "true"
        run: |
          # The guarded evaluator and hermetic Cargo path must pin offline mode.
          grep -R 'CARGO_NET_OFFLINE\|--offline\|--frozen' -n \
            src/rsi_bridge.rs crates/ccos-rsi/src crates/ccos-sandbox/src \
            crates/forge/forge-core/src/isolation.rs | head -20
          cargo test --release -p ccos-research-lab --features rsi --locked rsi_bridge -- --nocapture

      # ── Expansion contract (charter §42): the following classes MUST be
      # added as the sandbox grows, each as a named job step:
      #   timeout tests · descendant-process termination · live memory-limit ·
      #   live CPU-limit · live process-limit · output-limit · symlink tests ·
      #   path-traversal tests · evaluator-modification detection ·
      #   test-modification detection · Docker-access denial ·
      #   SSH-access denial · exfiltration tests · atomic-promotion tests.
      # Resource-limit argument construction is unit-tested now; the remaining
      # "live" cases require a runner on which Bubblewrap user namespaces are
      # guaranteed rather than merely fail-closed when unavailable.
      # A step is added only with a failing-first test proving the control.

name: Rebase PR1 on current main

on: workflow_dispatch

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "PR1 helper paused while workflow files are synchronized through the GitHub API"

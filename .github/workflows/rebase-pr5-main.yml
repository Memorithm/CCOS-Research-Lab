name: Rebase PR5 on current main

on: workflow_dispatch

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "PR5 helper paused while CI workflow is synchronized through the GitHub API"

[package]
name = "scirust-agent-protocol"
version = "0.1.0"
edition = "2021"
publish = false
rust-version = "1.89"
description = "Pinned SciRust execution-attestation protocol mirror for CCOS Research Lab"
license-file = "../../LICENSE.md"
repository = "https://github.com/Memorithm/scirust"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
